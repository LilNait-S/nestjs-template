version: '3.8'
services:
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: Nait
      POSTGRES_PASSWORD: lilnaitpassword
      POSTGRES_DB: nestdb
    ports:
      - 5499:5432
